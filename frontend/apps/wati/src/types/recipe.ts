// Re-export all types from @wati/types for backwards compatibility.
// New code should import directly from '@wati/types'.
export type { Tag, Ingredient, Recipe, RecipeSearchResponse } from '@wati/types';
